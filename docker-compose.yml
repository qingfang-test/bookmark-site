services:
  bookmarks:
    build: .
    container_name: bookmarks-app
    ports:
      - "5000:5000"
    environment:
      - DB_PATH=/data/bookmarks.db
    volumes:
      - ./data:/data
    restart: unless-stopped
